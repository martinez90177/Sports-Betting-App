# Design archive — pre-v2 source material

Kept because these were **inputs** to the v2 redesign, not outputs of it. They are
not superseded by `design_handoff_proppalace_v2/` and they are not instructions
to build anything.

## reference/

Captures of the app as it looked before the redesign, used as the "here is what
it does today" comparison while the v2 screens were designed.

| File | What it shows |
|---|---|
| `app-screens.html` | The main app screens, including the Settings card the v2 settings page was built from |
| `mobile.html` | The original 390px screens |
| `my-picks.html` | My Picks / ledger |
| `news.html` | News before the redesign |
| `alt-lines.html` | The alt-line ladder |

Originally at `design_handoff_proppalace_redesign/reference/` in the repo. Copied
here so the other handoff folders can be deleted without losing the before-state.

## Also worth keeping alongside these

- `LANDING_PAGE_BRIEF.md` — the original written brief
- `PropPalace Concepts.dc.html` — the 1a / 2a / 3a concept cards the visual
  direction was chosen from

You said you already pulled those two out into your own folder; move them in here
if you want everything in one place.
